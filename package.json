{
  "name": "append-log",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "rm -rf data.log && pnpm build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3"
  }
}